import datetime
import time


# a job scheduled to run every 10 seconds
while(True):
    time.sleep(10)  # 10 seconds.
    print("farah")  #to print the id of the records added in the collection(for debugging, this value can be observed in the heroku logs)
